import { Link } from 'react-router-dom'; import SiteHeader from '../../components/SiteHeader'; import SiteFooter from '../../components/SiteFooter'; import Protected from '../../components/Protected'; import {useAuth} from '../../context/AuthContext';
export default function ProfilePage(){return <Protected><SiteHeader/><main className="section"><div className="container profile-card"><div className="form-card"><div className="eyebrow">CUENTA</div><h1>Mi perfil</h1><ProfileContent/></div></div></main><SiteFooter/></Protected>}
function ProfileContent(){const {user}=useAuth();return <><p>Aquí podrás administrar tu información. En la siguiente fase esta pantalla se conectará con Laravel.</p><div className="list"><li><strong>Nombre</strong><br/>{user?.name}</li><li><strong>Correo</strong><br/>{user?.email}</li><li><strong>Rol</strong><br/>{user?.role}</li></div><div style={{marginTop:20}}><Link className="button" to="/deportivo">Ir a mis experiencias</Link></div></>}
