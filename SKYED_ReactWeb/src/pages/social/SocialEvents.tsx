import {useMemo,useState} from 'react'; import SiteHeader from '../../components/SiteHeader'; import SiteFooter from '../../components/SiteFooter'; import {socialEvents} from '../../data/mock'; import {SocialEventCard} from '../../components/EventCard';
export default function SocialEvents(){const [filter,setFilter]=useState('Todos');const cats=['Todos','Bodas','Quinceañeras','Cumpleaños','Corporativos','Baby shower'];const items=useMemo(()=>filter==='Todos'?socialEvents:socialEvents.filter(e=>e.category===filter),[filter]);return <div className="social-page"><SiteHeader brand="social"/><main><section className="page-hero social-hero"><div className="container"><span className="eyebrow social">SKYED SOCIAL</span><h1>Nuestros eventos</h1><p>Desde bodas íntimas hasta recepciones espectaculares — personalizamos cada detalle a tu medida.</p></div></section><section className="section"><div className="container"><div className="filter-bar">{cats.map(c=><button key={c} className={`filter ${filter===c?'active':''}`} onClick={()=>setFilter(c)}>{c}</button>)}</div><div className="grid grid-3">{items.map(e=><SocialEventCard key={e.id} event={e}/>)}</div></div></section></main><SiteFooter variant="social"/></div>}
