import {useParams,Link} from 'react-router-dom'; import SiteHeader from '../../components/SiteHeader'; import SiteFooter from '../../components/SiteFooter'; import {sportEvents} from '../../data/mock'; import Protected from '../../components/Protected';
export default function SportEventDetail(){const {id}=useParams();const event=sportEvents.find(e=>e.id===Number(id))??sportEvents[0];return <><SiteHeader brand="sport"/><main><section className="page-hero deportivo-hero"><div className="container"><span className="eyebrow">{event.category}</span><h1>{event.title}</h1><p>{event.date} · {event.location}</p></div></section><section className="section"><div className="container split"><div className="image-panel"><img src={event.image} alt={event.title}/></div><div><span className="eyebrow">SOBRE EL EVENTO</span><h2>{event.title}</h2><p>{event.description}</p><ul className="list"><li><strong>Fecha</strong><br/>{event.date}</li><li><strong>Ubicación</strong><br/>{event.location}</li><li><strong>Cupos</strong><br/>{event.capacity} participantes</li><li><strong>Inscripción</strong><br/>${event.price.toLocaleString('es-CO')}</li></ul><div style={{marginTop:20,display:'flex',gap:10,flexWrap:'wrap'}}><Link className="button" to={`/deportivo/inscripcion/${event.id}`}>Inscribirme</Link><Link className="button outline" to="/deportivo/eventos">Volver a eventos</Link></div></div></div></section><section className="section alt"><div className="container grid grid-3"><div className="feature"><h3>Requisitos</h3><p>Documento de identidad, aceptación de condiciones y equipo de seguridad.</p></div><div className="feature"><h3>Kit del evento</h3><p>Consulta tallas, entrega y disponibilidad antes de tu participación.</p></div><div className="feature"><h3>Soporte</h3><p>Contaremos con puntos de hidratación y soporte durante la jornada.</p></div></div></section></main><SiteFooter variant="sport"/></>}
