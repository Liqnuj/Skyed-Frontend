import {useMemo,useState} from 'react'; import SiteHeader from '../../components/SiteHeader'; import SiteFooter from '../../components/SiteFooter'; import {sportEvents} from '../../data/mock'; import {SportEventCard} from '../../components/EventCard';
export default function SportEvents(){const [filter,setFilter]=useState('Todos');const cats=['Todos','Ruta','MTB','Gravel','Pista','BMX'];const items=useMemo(()=>filter==='Todos'?sportEvents:sportEvents.filter(e=>e.category===filter),[filter]);return <><SiteHeader brand="sport"/><main><section className="page-hero deportivo-hero"><div className="container"><span className="eyebrow">SKYED DEPORTIVO</span><h1>Nuestros eventos</h1><p>Descubre las próximas competencias y rodadas de la temporada e inscríbete con un clic.</p></div></section><section className="section"><div className="container"><div className="filter-bar">{cats.map(c=><button key={c} className={`filter ${filter===c?'active':''}`} onClick={()=>setFilter(c)}>{c}</button>)}</div><div className="grid grid-3">{items.map(e=><SportEventCard key={e.id} event={e}/>)}</div></div></section></main><SiteFooter variant="sport"/></>}
