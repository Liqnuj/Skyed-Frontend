import {useState} from 'react'; import {Link} from 'react-router-dom'; import SiteHeader from '../../components/SiteHeader'; import SiteFooter from '../../components/SiteFooter'; import Protected from '../../components/Protected'; import {sportEvents} from '../../data/mock';
export default function Checkout(){const event=sportEvents[0];const [paid,setPaid]=useState(false);return <Protected><SiteHeader brand="sport"/><main className="section"><div className="container form-shell"><div className="form-card"><div className="eyebrow">INSCRIPCIÓN Y PAGO</div><h1>Confirma tu inscripción</h1><p>{event.title}</p>{paid?<><div className="form-success">Pago simulado aprobado. El pago real se conectará posteriormente a Laravel.</div><Link className="button" to="/deportivo/mi-entrada">Continuar a mi entrada</Link></>:<><div className="list"><li>Inscripción <strong style={{float:'right'}}>${event.price.toLocaleString('es-CO')}</strong></li><li>Método <strong style={{float:'right'}}>Pago seguro (demo)</strong></li></div><button className="button" style={{marginTop:18}} onClick={()=>setPaid(true)}>Confirmar pago</button></>}</div></div></main><SiteFooter variant="sport"/></Protected>}
