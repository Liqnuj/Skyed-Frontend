import {Link} from 'react-router-dom'; import SiteHeader from '../../components/SiteHeader'; import SiteFooter from '../../components/SiteFooter'; import Protected from '../../components/Protected'; import {sportEvents} from '../../data/mock';
export default function MyEntry(){const raw=localStorage.getItem('skyed_inscription');const data=raw?JSON.parse(raw):null;const event=sportEvents.find(e=>e.id===data?.eventId);return <Protected><SiteHeader brand="sport"/><main className="section"><div className="container form-shell"><div className="form-card">{event?<><div className="eyebrow">ENTRADA DIGITAL</div><h1>Tu entrada digital</h1><div className="callout" style={{marginTop:20}}><h2>{event.title}</h2><p>{event.date} · {event.location}</p><div style={{fontSize:'3rem',letterSpacing:'.3rem',margin:'20px 0'}}>▦▦▦</div><p>Esta representación es local. El QR real se generará desde Laravel.</p></div></>:<div className="empty">Todavía no tienes una inscripción. <Link to="/deportivo/eventos">Ver eventos</Link></div>}</div></div></main><SiteFooter variant="sport"/></Protected>}
